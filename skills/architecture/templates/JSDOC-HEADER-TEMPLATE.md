<!-- See skills/architecture/SKILL.md for how to fill -->

Top-of-file JSDoc for every entry-point in `src/`. Max 5 content lines; if more is needed, write a module README and link it.

```ts
/**
 * @module {concept-name-from-architecture-map}
 *
 * Related files:
 *   - {relative/path/to/related-file.ts}: {one-line role}
 *
 * ADR: ADR-{nn} ({short title})
 * Map row: src/ARCHITECTURE.map -> "{concept-name}"
 */
```
